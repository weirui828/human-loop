pandas>=2.0.0
numpy>=1.24.0
# Pinned: the serialized pipelines in models/ are built with this version.
# An unpinned range let the env drift to 1.9.0 and every model load emitted
# "InconsistentVersionWarning ... may lead to invalid results". Results were
# verified identical to 4dp across versions, but the warning is not worth
# re-earning -- and a genuine breaking change would be silent.
scikit-learn==1.8.0
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0
jupyter>=1.0.0
ipykernel>=6.25.0
nbconvert>=7.0.0
joblib>=1.3.0
requests>=2.31.0
python-dotenv>=1.0.0
streamlit>=1.30.0

